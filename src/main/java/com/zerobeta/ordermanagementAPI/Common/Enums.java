package com.zerobeta.ordermanagementAPI.Common;

public class Enums {

    // This enum is used to represent the status of an order
    public enum OrderStatus {
        NEW, DISPATCHED, CANCELLED
    }
}
